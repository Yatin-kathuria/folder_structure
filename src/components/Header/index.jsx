import React from 'react'
import { Box } from '@mui/material'

const Header = () => {
  return (
    <Box display={'flex'} alignItems={'center'} justifyContent={'center'} p={3} sx={{backgroundColor: 'primary.light'}}>Nested Folder Structre</Box>
  )
}

export default Header